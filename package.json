{
  "name": "logicpilot",
  "version": "0.0.1",
  "private": true,
  "description": "LogicPilot simulation OS monorepo root",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "codegen": "node scripts/codegen-ts.mjs",
    "format": "prettier --write \"web/**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"web/**/*.{ts,tsx,json,md}\"",
    "lint": "eslint \"web/**/*.{ts,tsx}\"",
    "test": "echo test placeholder: vitest wiring lands with web scaffolding",
    "build": "echo build placeholder: pnpm -r run build once web packages exist"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
