// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { EquationVariable } from '../../logicpilot/ir/equation-variable.js';
import { Metadata } from '../../logicpilot/ir/metadata.js';
import { Param } from '../../logicpilot/ir/param.js';


export class EquationModel {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EquationModel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEquationModel(bb:flatbuffers.ByteBuffer, obj?:EquationModel):EquationModel {
  return (obj || new EquationModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEquationModel(bb:flatbuffers.ByteBuffer, obj?:EquationModel):EquationModel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EquationModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

metadata(obj?:Metadata):Metadata|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Metadata()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

variables(index: number, obj?:EquationVariable):EquationVariable|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new EquationVariable()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

variablesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

equations(index: number):string
equations(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
equations(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

equationsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

params(index: number, obj?:Param):Param|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Param()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startEquationModel(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addMetadata(builder:flatbuffers.Builder, metadataOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, metadataOffset, 0);
}

static addVariables(builder:flatbuffers.Builder, variablesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, variablesOffset, 0);
}

static createVariablesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startVariablesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addEquations(builder:flatbuffers.Builder, equationsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, equationsOffset, 0);
}

static createEquationsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEquationsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endEquationModel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createEquationModel(builder:flatbuffers.Builder, metadataOffset:flatbuffers.Offset, variablesOffset:flatbuffers.Offset, equationsOffset:flatbuffers.Offset, paramsOffset:flatbuffers.Offset):flatbuffers.Offset {
  EquationModel.startEquationModel(builder);
  EquationModel.addMetadata(builder, metadataOffset);
  EquationModel.addVariables(builder, variablesOffset);
  EquationModel.addEquations(builder, equationsOffset);
  EquationModel.addParams(builder, paramsOffset);
  return EquationModel.endEquationModel(builder);
}
}
