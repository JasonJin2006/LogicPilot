// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Coupling } from '../../logicpilot/ir/coupling.js';
import { Metadata } from '../../logicpilot/ir/metadata.js';
import { Param } from '../../logicpilot/ir/param.js';
import { ProcessNode } from '../../logicpilot/ir/process-node.js';


export class ProcessModel {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ProcessModel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsProcessModel(bb:flatbuffers.ByteBuffer, obj?:ProcessModel):ProcessModel {
  return (obj || new ProcessModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsProcessModel(bb:flatbuffers.ByteBuffer, obj?:ProcessModel):ProcessModel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ProcessModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

metadata(obj?:Metadata):Metadata|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Metadata()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

nodes(index: number, obj?:ProcessNode):ProcessNode|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new ProcessNode()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

nodesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

couplings(index: number, obj?:Coupling):Coupling|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Coupling()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

couplingsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

params(index: number, obj?:Param):Param|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Param()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startProcessModel(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addMetadata(builder:flatbuffers.Builder, metadataOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, metadataOffset, 0);
}

static addNodes(builder:flatbuffers.Builder, nodesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nodesOffset, 0);
}

static createNodesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startNodesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCouplings(builder:flatbuffers.Builder, couplingsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, couplingsOffset, 0);
}

static createCouplingsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCouplingsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endProcessModel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createProcessModel(builder:flatbuffers.Builder, metadataOffset:flatbuffers.Offset, nodesOffset:flatbuffers.Offset, couplingsOffset:flatbuffers.Offset, paramsOffset:flatbuffers.Offset):flatbuffers.Offset {
  ProcessModel.startProcessModel(builder);
  ProcessModel.addMetadata(builder, metadataOffset);
  ProcessModel.addNodes(builder, nodesOffset);
  ProcessModel.addCouplings(builder, couplingsOffset);
  ProcessModel.addParams(builder, paramsOffset);
  return ProcessModel.endProcessModel(builder);
}
}
