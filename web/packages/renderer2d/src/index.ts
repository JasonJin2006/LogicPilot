// @logicpilot/renderer2d — public entry point.
//
// Pure, DOM-free building blocks for the LogicPilot browser visualization.
// Keeping the decode path here (instead of inside the IDE app) leaves the
// door open for moving it into a Web Worker later.

export * from './decode.js';
